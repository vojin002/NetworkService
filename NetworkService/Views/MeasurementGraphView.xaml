<UserControl x:Class="NetworkService.Views.MeasurementGraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PanelBrush}"
                BorderThickness="3,1,1,1" Margin="0,0,0,10" CornerRadius="4" Padding="12">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="{StaticResource AccentColor}" Offset="0"/>
                    <GradientStop Color="{StaticResource AccentColor}" Offset="0.01"/>
                    <GradientStop Color="{StaticResource BorderColor}" Offset="0.01"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Sensor:" Style="{StaticResource LabelStyle}" Margin="0,0,10,0"/>
                <ComboBox x:Name="SensorComboBox"
                          ItemsSource="{Binding SensorList}"
                          SelectedItem="{Binding SelectedSensor}"
                          DisplayMemberPath="Name"
                          Width="200"/>
                <Button Content="Refresh"
                        Command="{Binding RefreshGraphCommand}"
                        Style="{StaticResource ActionButtonStyle}"
                        Margin="10,0,0,0"
                        ToolTip="Manually refresh the graph"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="4">
            <Canvas x:Name="GraphCanvas" Margin="20"/>
        </Border>
    </Grid>
</UserControl>
