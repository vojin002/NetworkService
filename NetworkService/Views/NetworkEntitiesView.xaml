<UserControl x:Class="NetworkService.Views.NetworkEntitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NetworkService.Views"
             Background="{StaticResource BackgroundBrush}">
    <UserControl.Resources>
        <local:NullToNAConverter x:Key="NullToNA"/>
    </UserControl.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PanelBrush}"
                BorderThickness="3,1,1,1"
                Padding="12" Margin="0,0,0,10" CornerRadius="4">
            <Border.Effect>
                <DropShadowEffect Color="#CBD5E0" BlurRadius="4" ShadowDepth="1" Direction="270" Opacity="0.4"/>
            </Border.Effect>
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="{StaticResource AccentColor}" Offset="0"/>
                    <GradientStop Color="{StaticResource AccentColor}" Offset="0.01"/>
                    <GradientStop Color="{StaticResource BorderColor}" Offset="0.01"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <StackPanel>
                <TextBlock Text="Add Sensor" Style="{StaticResource SectionHeaderStyle}"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Type:" Style="{StaticResource LabelStyle}" Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding AvailableTypes}"
                              SelectedItem="{Binding SelectedTypeToAdd}"
                              DisplayMemberPath="Name"
                              Width="160"
                              ToolTip="Select the type of sensor to add"/>
                    <Button Content="Add Sensor"
                            Command="{Binding AddSensorCommand}"
                            Style="{StaticResource ActionButtonStyle}"
                            Margin="12,0,0,0"
                            ToolTip="Add a new sensor of the selected type"/>
                </StackPanel>
                <TextBlock Text="{Binding TypeError}" Style="{StaticResource ErrorStyle}"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource PanelBrush}"
                BorderThickness="3,1,1,1"
                Padding="12" Margin="0,0,0,10" CornerRadius="4">
            <Border.Effect>
                <DropShadowEffect Color="#CBD5E0" BlurRadius="4" ShadowDepth="1" Direction="270" Opacity="0.4"/>
            </Border.Effect>
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="{StaticResource AccentColor}" Offset="0"/>
                    <GradientStop Color="{StaticResource AccentColor}" Offset="0.01"/>
                    <GradientStop Color="{StaticResource BorderColor}" Offset="0.01"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <StackPanel>
                <TextBlock Text="Search" Style="{StaticResource SectionHeaderStyle}"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <RadioButton Content="By Name" IsChecked="{Binding SearchByName}"
                                 Margin="0,0,15,0"
                                 ToolTip="Search sensors by name"/>
                    <RadioButton Content="By Type" IsChecked="{Binding SearchByType}"
                                 ToolTip="Search sensors by type"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Width="200" Style="{StaticResource InputStyle}"
                             ToolTip="Enter search term"/>
                    <Button Content="Search" Command="{Binding SearchCommand}"
                            Style="{StaticResource ActionButtonStyle}" Margin="8,0,0,0"
                            ToolTip="Apply search"/>
                    <Button Content="Clear" Command="{Binding ClearSearchCommand}"
                            Style="{StaticResource NavButtonStyle}" Margin="6,0,0,0"
                            ToolTip="Clear search and show all sensors"/>
                    <Button Content="Save Search" Command="{Binding SaveSearchCommand}"
                            Style="{StaticResource NavButtonStyle}" Margin="6,0,0,0"
                            ToolTip="Save this search for later use"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Saved searches:" Style="{StaticResource LabelStyle}" Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding SavedSearches}"
                              SelectedItem="{Binding SelectedSavedSearch}"
                              Width="200"
                              ToolTip="Select a saved search to apply it"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Button Content="Delete Selected"
                    Command="{Binding DeleteSensorCommand}"
                    HorizontalAlignment="Left"
                    ToolTip="Delete the selected sensor from the list">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource OutlinedDangerButtonStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ShowDeleteConfirmation}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ShowDeleteConfirmation}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock Text="Delete this sensor?" Style="{StaticResource LabelStyle}" Margin="0,0,12,0"/>
                <Button Content="Yes" Command="{Binding ConfirmDeleteCommand}"
                        Style="{StaticResource DangerButtonStyle}" Margin="0,0,6,0"/>
                <Button Content="No" Command="{Binding CancelDeleteCommand}"
                        Style="{StaticResource NavButtonStyle}"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding FilteredSensors}"
                  SelectedItem="{Binding SelectedSensor}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserDeleteRows="False"
                  CanUserAddRows="False"
                  Background="{StaticResource PanelBrush}"
                  Foreground="{StaticResource TextBrush}"
                  BorderBrush="{StaticResource BorderBrush}"
                  RowBackground="#FFFFFF"
                  AlternatingRowBackground="#F0F4F8"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  RowHeight="34">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#E3F2FD"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#BBDEFB"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{StaticResource BorderBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="8,6"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="8,0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="160">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="8,0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Type" Binding="{Binding Type.Name}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="8,0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Last Value (°C)" Binding="{Binding LastMeasuredValue, Converter={StaticResource NullToNA}}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="8,0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="Status" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                                <Ellipse Width="8" Height="8" Margin="0,0,6,0" VerticalAlignment="Center">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Setter Property="Fill" Value="#009664"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsValueValid}" Value="False">
                                                    <Setter Property="Fill" Value="{StaticResource DangerBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                <TextBlock VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="OK"/>
                                            <Setter Property="Foreground" Value="#009664"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsValueValid}" Value="False">
                                                    <Setter Property="Text" Value="OUT OF RANGE"/>
                                                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
