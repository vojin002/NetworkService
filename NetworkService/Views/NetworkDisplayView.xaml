<UserControl x:Class="NetworkService.Views.NetworkDisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Auto arrange button -->
        <Button Grid.Row="0" Grid.ColumnSpan="2"
                Content="Auto Arrange All"
                Command="{Binding AutoArrangeCommand}"
                Style="{StaticResource ActionButtonStyle}"
                HorizontalAlignment="Left"
                Margin="0,0,0,8"
                ToolTip="Automatically place all sensors on the grid"/>

        <!-- TreeView with sensors grouped by type -->
        <Border Grid.Row="1" Grid.RowSpan="2" Grid.Column="0"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" Margin="0,0,8,0" CornerRadius="4">
            <StackPanel>
                <TextBlock Text="Sensors" Style="{StaticResource SectionHeaderStyle}"
                           Margin="8,8,8,4" FontSize="13"/>
                <TreeView x:Name="SensorTreeView"
                          ItemsSource="{Binding SensorsInTreeView}"
                          Background="Transparent"
                          Foreground="{StaticResource TextBrush}"
                          BorderThickness="0"
                          PreviewMouseLeftButtonDown="TreeView_PreviewMouseLeftButtonDown">
                    <TreeView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"
                                       Foreground="{StaticResource TextBrush}"
                                       Padding="2" FontSize="12"
                                       Cursor="Hand"
                                       ToolTip="Drag to place on grid"/>
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </StackPanel>
        </Border>

        <!-- Canvas grid 4x3 = 12 slots -->
        <Border Grid.Row="1" Grid.RowSpan="2" Grid.Column="1"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="4">
            <UniformGrid x:Name="CanvasGrid" Rows="3" Columns="4"
                         AllowDrop="True"
                         DragOver="CanvasGrid_DragOver"
                         Drop="CanvasGrid_Drop">
                <!-- 12 canvas slots created in code-behind -->
            </UniformGrid>
        </Border>
    </Grid>
</UserControl>
